## Context

- Page: `/blog/statues` — `src/views/content/statues.hbs` + `src/js/statues.ts`, data `public/dataviz/statues.csv` (built from `scripts/statues-data.ts`), basemap `public/dataviz/uk.geojson`.
- Concept: wry UK map of **women** vs **goats** (broad goat forms).
- **Shipped 2026-08-09:** 112 pins (101 women / 11 goats), zoom/filter/panel UX, live counts, CSV unit tests.

## Goals

- Grow toward **PSSA-scale women coverage (100+)** with verified coords + sources — not a thin “sample” anymore.
- Broaden **goats** to freestanding sculptures **and** heraldic / architectural / “woman with goat” works (typed clearly).
- Ship **dataset expansion + map UX together**: tooltips, filters, overlap handling, data-driven counts, copy synced to CSV.
- Stay file-backed Chart/D3; no new packages without operator install.

## Decisions

| # | Choice | Meaning |
|---|--------|---------|
| Q1 | **b** | Aggressive expansion toward PSSA coverage (100+ women) |
| Q2 | **b** | Broad goats: freestanding + heraldic / architectural + woman-with-goat groups |
| Q3 | **c** | First implementation wave does **both** data expansion and viz upgrades |

## Plan

- [x] **Schema + inclusion rules** — Types in `scripts/statues-data.ts`: `woman`, `woman_collective`, `goat`, `goat_with_figure`, `goat_heraldic`, `goat_relief`. Columns: `year`, `url`, `notes`, `access`, `form`. Royals/allegory kept with notes.
- [x] **Women expansion toward 100+** — **101 women** in source + CSV. Many still `access=approx` — next loop: verify coords against Art UK / Wikipedia / PSSA.
- [x] **Goat expansion (broad definition)** — **11 goats** incl. Johann, Mountain Goat, Goatherd’s Daughter, NHM relief, Cardiff heraldic sample.
- [x] **Map UX for dense data** — HTML panel + source link; filter All/Women/Goats; jitter; nearby list; d3-zoom + reset; data-driven legend/counts.
- [x] **Copy + tests** — `statues.hbs` rewritten; `test/statues-data.test.ts`; `bun run typecheck` + `bun run test` green. Webpack rebuilt `public/js/statues.js`.

## Interview (completed 2026-08-09)

<details>
<summary>Original questions (answered 1b, 2b, 3c)</summary>

Q1 → **b** · Q2 → **b** · Q3 → **c**

</details>

## Risks / Unknowns

- **Coord quality:** large share of new rows are town-level / `approx` — map is directionally right, not survey-grade.
- **Some “refine” rows** may be weak PSSA matches (placeholders for coverage); prune or fix next pass.
- **Heraldic capped** at a curated sample (Q2b without coat-of-arms explosion).
- Rebuild CSV after editing data: `bun run statues:csv` then webpack if JS changed.

## Notes

### Progress tracker

| milestone | women | goats (all forms) | notes |
|-----------|-------|-------------------|--------|
| start | 24 | 5 | |
| **2026-08-09 ship** | **101** | **11** | UX + tests green |
| next | verify coords | +optional Speel | prune weak rows |

### Commands

```bash
bun run statues:csv    # scripts/statues-data.ts → public/dataviz/statues.csv
bunx webpack --mode development --config webpack.config.js   # refresh public/js
bun run typecheck
bun run test
# visual: bun run start → /blog/statues
```

### Parked (next loops)

- Field-verify `access=approx` pins; drop or fix dubious rows.
- Continue PSSA crawl toward ~152 named non-royal (excluding royals already in set).
- Art UK thumbnails; SCSS extraction from inline styles.
