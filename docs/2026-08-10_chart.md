# Chart rebuild + dataviz workflow

**Date:** 2026-08-10  
**Status:** First wave shipped (schema + Chart split). See also `docs/dataviz-workflow.md`.

---

## Context

- Chart was a ~2.2k-line workbench. Rebuild targets an **upstreamable slim core** (lighter than Highcharts, reusable outside this site) plus domain modules.
- **Core six:** georgia, earthquake, winamp, matrix, breathe, war (+ genuary).

## Goals

1. Slim, intentional Chart core suitable to upstream / import elsewhere.
2. Lifecycle `status` for listing vs CI vs WIP vs private dumps.
3. Workflow: idea → sandbox → publish → lessons → upstream → re-check published.

## Decisions

| # | Choice | Meaning |
|---|--------|---------|
| Q1 | **b** (+ intent) | Split modules; Chart core upstream-oriented |
| Q2 | **a** | `status: published \| hold \| private \| archived` |
| Q3 | **a** | winamp → published + `tier: core` |
| Q4 | **c** | Tags + Chart rebuild in one wave |

## Plan

- [x] **Lifecycle schema** — `config/blogpost-types.ts`; migrate `blogposts.ts`; controllers use `isListed`; winamp promoted; tests updated.
- [x] **Chart inventory + split** — `chart.ts` slim core; `chart-map.ts` (`MapChart`); `chart-extras.ts` (treemap / generalisedLineChart for archived); Georgia alert → optional `onContain`.
- [x] **Consumer updates** — georgia/weapons → MapChart; AusIncome/files/preflight/monthly/slurm → ExtrasChart; stale `chart.d.ts` removed.
- [x] **Regression** — typecheck + `bun run test` green; webpack rebuilt.
- [x] **Workflow doc** — `docs/dataviz-workflow.md`.

## Interview (completed)

<details>
<summary>1b (+ upstream), 2a, 3a, 4c</summary>

Q1 → split modules, upstreamable core · Q2 → status enum · Q3 → winamp published · Q4 → both in one wave

</details>

## Notes / next

- Harden `hold` / `private` HTTP behaviour (WIP banner vs 404) if needed.
- Drop lodash/`$` barrel from Chart when ready (import directly in posts).
- Coordinate-quality pass on statues still separate (`docs/2026-08-09_statues.md`).
- Monolith backup: `docs/_chart_monolith.ts.bak` (optional delete later).

```bash
bun run typecheck && bun run test
bunx webpack --mode development --config webpack.config.js
```
