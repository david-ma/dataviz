# Wealth blogpost refresh

Date: 2026-08-10

## Was broken / dated

- `@ts-nocheck`, console noise, local `classifyName` duplicate
- Region nodes never filled `children` (only `countries`) — accidental leaf layout
- d3 v6+ handlers still used `(d)` instead of `(event, d)` — zoom/highlight unreliable
- Opacity used nonexistent `parent.total` / `data.value`
- CSV download pointed at `/wealth/WorldWealth.csv` (404); data is `/blogposts/WorldWealth.csv`
- Chart title + page `h1` both inside `#chart`
- `decorateTable` always did `Object.keys(dataset[0])` even when `columns` was passed → crash on empty/failed CSV load
- CSV under `/blogposts/*.csv` needs **`?raw=true`** for d3 to get plain text (without it the response is not usable as CSV → empty parse → table crash). Operator fixed: `CSV_URL = '/blogposts/WorldWealth.csv?raw=true'`.

## Now

- Typed hierarchy World → Region → Country; scratchpad redraw for zoom
- Legend + reset; summary counts; copy/layout closer to statues
- `decorateTable` prefers explicit `columns` and tolerates empty data
- CSV load errors surface in the summary line instead of throwing
- Rebuild: `bun run build:dev` then `bun run preview` → `/blog/wealth`
