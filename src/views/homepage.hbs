<div id="vizPreviews">
  {{#blogposts}}
  <div class="preview col-xl-3 col-lg-4 col-md-6 col-xs-12">
    <h2>{{title}}</h2>
    <span class="subtitle">{{category}} {{publish_date}}</span>
    <p>{{summary}}</p>
    <a href="/blog/{{shortname}}"><img src="{{image}}"></a>
  </div>
  {{/blogposts}}
</div>